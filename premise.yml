name: premise
channels:
    - conda-forge
dependencies:
    # core functionality
    - python=3.10
    # data science
    - pandas
    - numpy
    - scipy
    # jupyter
    - jupyterlab
    # lca
    - cmutel::brightway2_nosolver
    - scikit-umfpack
    - pip
    - pip:
        - premise