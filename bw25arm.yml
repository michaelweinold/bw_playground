name: bw25arm
channels:
    - conda-forge
dependencies:
    # core functionality
    - python=3.11
    # data science
    - pandas
    - numpy
    - scipy
    # jupyter
    - jupyterlab
    # visualization
    - seaborn
    # lca
    - cmutel::brightway25_nosolver
    - cmutel::bw_processing
    - scikit-umfpack
